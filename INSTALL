In order to install this on your local machine, you can use the setup.py file
that is provided in this directory.  Execute the following command with the
appropriate privileges required to install packages on your machine.

    python setup.py install
